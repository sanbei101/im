引入redis作为im高频操作的meta存储

涉及到:
+ 群成员列表: im:{room:101}:members
+ 用户加入的room(私聊也是一个room): im:{user:1001}:rooms
+ 群禁言: im:{room:101}:muted
+ 未读消息计数器: im:{user:1001}:unread
+ 控制广播: im:events:control(用于api对gateway同步数据),需要确定`payload`结构
+ 网关路由定位表: im:user:{user_1001}:gateway


gateway 鉴权大致结构:
```
[网关收到消息: sender_id -> room_id]
                                      │
                         IsPrivateRoom(room_id)? 
                         (判断前缀是否为 p2p:)
                                      │
               ┌──────────────────────┴──────────────────────┐
               ▼ (是 1v1 私聊)                               ▼ (是群聊)
   1. 校验 room_id 是否包含 sender_id            1. 查网关本地 Session 或 Redis
      (不包含 ──► 拒发，非本人房间)             `im:user:{sender_id}:rooms`
               │                                   (不在群里 ──► 拒发)
               ▼                                             │
   2. 查接收方黑名单                                         ▼
      `SISMEMBER im:user:{target}:blocklist`    2. 查群禁言状态
      (存在 ──► 拒发，已被拉黑)                      查询 im:{room:ID}:muted
               │                                   (被禁言 ──► 拒发)
               ▼                                             │
      ┌────────┴─────────────────────────────────────────────┘
      ▼
   [通过校验，放行消息进入投递/落库流程]
```

## 策略:

### 1.解决“网关不能查 DB”与“删缓存导致网关 Cache Miss 无法鉴权”冲突的方案

API 侧的双写更新拆分为两条核心规则：

#### 1. 网关强依赖数据 → 主动同步更新

对于成员关系、黑名单、禁言状态，API 在写完 PostgreSQL 之后,直接调用 Redis 命令执行增量更新，随后抛出 Pub/Sub 广播。

- **加群 / 退群 / 踢人**
  1. 写 DB
  2. `ZADD` / `ZREM` `im:{room:ID}:members` `<timestamp>` `<user_id>`
  3. `ZADD` / `ZREM` `im:user:{user_id}:rooms` `<timestamp>` `<room_id>`
  4. `PUBLISH` `im:events:control` `<payload>`

- **禁言 / 解禁**
  1. 写 DB
  2. 更新 `im:{room:ID}:muted`
  3. `PUBLISH` `im:events:control` `<payload>`

- **拉黑 / 取消拉黑**
  1. 写 DB
  2. `SADD` / `SREM` `im:user:{user_id}:blocklist` `<target_user_id>`

---

#### 2. API 展示类非核心数据 → 删除缓存 / 延迟写入(这部分先不实现)

对于用户个人资料（昵称、头像）、群公告、群元数据等仅在 HTTP 界面展示的数据：

- **写 DB** → `DEL` `im:user:{user_id}:profile`

后续客户端请求 API 时，若 API 查 DB 查不到，则再回刷 Redis。

+ gateway 只能连接redis,不能直接连接postgres,在收到用户消息的适合,需要校验相关权限(是否被禁言,是否被踢出,是否不是好友...)
+
